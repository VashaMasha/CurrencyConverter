import React from 'react';
import CurrencyConversionStackNavigator from './CurrencyConversionStackNavigator';

const Router = (): React.JSX.Element => {
  return <CurrencyConversionStackNavigator />;
};

export default Router;
